namespace BusinessLogic {
    using System.Collections;
    using GameState;
    using Core;
    using Persistence;
    using UnityEngine;

    public class ScoringSystem : MonoBehaviour {
        // Scoring system has been completely removed per design decision.
        // This class is kept empty simply to prevent breaking any existing
        // scene prefab references that might have this component attached.
    }
}

